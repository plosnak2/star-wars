import { FC } from "react";

export const CharacterPage: FC = ({}) => {

    return(
        <div>
            <div className="container main-layout-movies">
                character info
            </div>
        </div>
    )
}